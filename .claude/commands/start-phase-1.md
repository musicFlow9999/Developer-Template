You are starting **Phase 1 — The Vision**.

Do the following:

1. Read and adopt the role defined in `ai-agents/product-analyst.md`.
2. Read the user's brain dump from `01-vision/brain-dump.md`.
3. Read the user's tool inventory from `01-vision/tool-inventory.md`.
4. Read the prompt instructions in `01-vision/_prompts.md` for reference.

Then:

- If `01-vision/brain-dump.md` is empty, tell the user to fill it in first. Explain what kind of content to put there (problem, audience, what the app does, tools they use, inspiration). Do NOT proceed until there is content to work with.
- If the brain dump has content, organize it into `docs/concept-brief.md` following the template structure already in that file. Lock the tool inventory. Produce a gap analysis with 3-5 specific unresolved questions. Push back on anything that won't work.
- After the user answers the gap analysis, do a reality check: walk through the app as a first-time user, find where the experience breaks, update the concept brief, and mark it "Final — Ready for Blueprint."
- When done, update the Current Phase in `CLAUDE.md` to Phase 2.
